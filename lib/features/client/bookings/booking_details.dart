import 'package:flutter/material.dart';

class ClientBookingDetails extends StatelessWidget {
  const ClientBookingDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}