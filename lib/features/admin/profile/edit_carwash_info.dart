import 'package:flutter/material.dart';

class EditCarwashInfo extends StatelessWidget {
  const EditCarwashInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(),);
  }
}