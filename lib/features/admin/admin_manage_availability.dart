import 'package:flutter/material.dart';

class AdminManageAvailability extends StatelessWidget {
  const AdminManageAvailability({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Admin Manage Availability"),),
    );
  }
}