import 'package:flutter/material.dart';

class AdminCarwashSetup extends StatelessWidget {
  const AdminCarwashSetup({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Admin CarWash setup"),),
    );
  }
}